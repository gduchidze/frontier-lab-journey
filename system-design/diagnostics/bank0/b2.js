// Phase 0 diagnostic bank - topic b2 - Deep learning foundations.
// Grounded in Karpathy's Neural Networks: Zero to Hero (videos 1-4,
// micrograd/makemore level). 20 questions: ~7 recall, ~9 scenario,
// ~4 numeric. Invariants: 3 options per question with equal word
// counts; answers 7/7/6 across 0/1/2, never 3 identical in a row.
var PHASE0_BANK_B2 = [
  { topic: 'b2', q: "In backpropagation, what does each node's local gradient represent?",
    options: ["The loss value computed at that node directly",
              "The derivative of node output versus node inputs",
              "The learning rate scaled by the node weight"],
    answer: 1, explain: "Each node stores d(output)/d(input); backprop multiplies these local gradients along the graph via the chain rule." },
  { topic: 'b2', q: "Why does backprop traverse the computation graph in reverse topological order?",
    options: ["Each node's gradient needs gradients from all downstream consumers",
              "Reverse order reduces memory usage during the forward pass",
              "Gradients are undefined until every weight has fully updated"],
    answer: 0, explain: "By the chain rule, dL/dnode is built from every consumer's gradient, so all consumers must be processed before the node itself." },
  { topic: 'b2', q: "For L = (a * b) + c with a = 2, b = 3, c = 4, what is dL/da?",
    options: ["dL/da equals two in total",
              "dL/da equals four in total",
              "dL/da equals three in total"],
    answer: 2, explain: "The addition passes the gradient through unchanged and the multiply's local gradient for a is b, so dL/da = b = 3." },
  { topic: 'b2', q: "An MLP maps 100 inputs to a 50-unit hidden layer, then to 10 outputs, with biases everywhere. Total parameter count?",
    options: ["5,560 parameters in total",
              "5,500 parameters in total",
              "5,010 parameters in total"],
    answer: 0, explain: "Weights 100x50 + 50x10 = 5,500 plus biases 50 + 10 = 60 gives 5,560." },
  { topic: 'b2', q: "Your loss shoots to NaN within the first few training steps. What do you check first?",
    options: ["Whether the network has too few layers",
              "Whether the learning rate is too high",
              "Whether the dataset is shuffled every epoch"],
    answer: 1, explain: "An oversized learning rate makes updates overshoot and amplify until activations explode and the loss overflows to NaN." },
  { topic: 'b2', q: "Plain SGD zigzags across a narrow ravine in the loss surface, making slow progress. What does adding momentum change?",
    options: ["It removes the need to choose any learning rate",
              "It damps oscillations and speeds progress along consistent directions",
              "It normalizes each parameter's gradient by its recent magnitude"],
    answer: 1, explain: "Momentum accumulates a velocity that cancels the zigzag components and accelerates along the persistent gradient direction; per-parameter scaling is Adam's job." },
  { topic: 'b2', q: "Some parameters receive huge gradients while rare embedding rows receive tiny ones. Which Adam feature addresses this imbalance?",
    options: ["A schedule that decays the learning rate automatically",
              "A hard clip applied to every gradient value",
              "A per-parameter step size from tracked squared gradients"],
    answer: 2, explain: "Adam keeps a second-moment estimate of squared gradients and divides each update by its root, adapting the step size per parameter." },
  { topic: 'b2', q: "A deep sigmoid MLP trains: the last layers learn but the first layers' weights barely move. Most likely cause?",
    options: ["Gradients vanish as they propagate backward repeatedly",
              "The first layers already reached optimal weights",
              "Early layers always require larger batch sizes"],
    answer: 0, explain: "Sigmoid's maximum slope is 0.25, so repeated multiplication through many layers shrinks gradients exponentially toward the input." },
  { topic: 'b2', q: "At initialization, your tanh activation histogram shows values pinned at -1 and +1, and loss barely improves. What went wrong?",
    options: ["Weights were initialized with excessive scale",
              "The learning rate is too small",
              "The dataset labels are mostly incorrect"],
    answer: 0, explain: "Oversized initial weights push tanh into its saturated tails where the local gradient is near zero, so learning stalls -- the classic makemore fix is scaling the weights down." },
  { topic: 'b2', q: "What is the core idea behind Xavier or Kaiming initialization?",
    options: ["Set all weights to zero so training starts symmetric",
              "Copy pretrained weights from another network into every layer",
              "Scale weights so activation variance stays stable across layers"],
    answer: 2, explain: "Scaling by roughly 1/sqrt(fan_in) keeps activations and gradients at unit-scale variance layer after layer, preventing early saturation or explosion." },
  { topic: 'b2', q: "During training, batch norm normalizes each activation using statistics computed over what?",
    options: ["All the features within one single example",
              "All the examples in the current mini-batch",
              "The entire training set seen so far"],
    answer: 1, explain: "Batch norm standardizes each channel using the current batch's mean and variance; normalizing across features within one example is layer norm." },
  { topic: 'b2', q: "A batch norm model predicts nonsense when served one request at a time. What should inference use instead of batch statistics?",
    options: ["Running mean and variance from training",
              "Statistics recomputed over the test set",
              "Fresh statistics from each single example"],
    answer: 0, explain: "A single example has no meaningful batch statistics, so batch norm must switch to the running averages accumulated during training -- the train/eval mode distinction." },
  { topic: 'b2', q: "Why do transformers and RNN-style models prefer layer norm over batch norm?",
    options: ["Layer norm trains faster because it skips computing variance",
              "Layer norm works per example, independent of batch composition",
              "Layer norm adds trainable parameters while batch norm cannot"],
    answer: 1, explain: "Layer norm normalizes across features within one example, so it behaves identically at any batch size and couples no examples together." },
  { topic: 'b2', q: "After training with a high learning rate, 40 percent of your ReLU units output zero for every input. What happened?",
    options: ["Those units saturated at their maximum value and stopped learning",
              "Batch norm rescaled those units toward a zero mean output",
              "They died: negative preactivations for all inputs give zero gradient"],
    answer: 2, explain: "A big update can knock a unit's preactivation negative for every input; ReLU then outputs zero with zero local gradient, so the unit can never recover." },
  { topic: 'b2', q: "Training loss fell early then plateaued well above zero, and validation loss tracks it closely. Which knob do you reach for first?",
    options: ["Decay the learning rate or add capacity",
              "Add dropout to reduce the obvious overfitting",
              "Collect more data to close the gap"],
    answer: 0, explain: "Train and validation moving together means underfitting, so lower the step size to settle deeper or grow the model; regularization targets a train-val gap that is not present." },
  { topic: 'b2', q: "You multiply the learning rate by 10 and the loss now oscillates wildly around a high value. What is happening?",
    options: ["The optimizer has found a sharper, better minimum",
              "Steps overshoot the minimum and bounce across it",
              "Gradients vanished, so parameters no longer move anywhere"],
    answer: 1, explain: "When the step size exceeds the loss surface's curvature scale, each update jumps past the valley floor and the loss oscillates or diverges instead of settling." },
  { topic: 'b2', q: "In micrograd terms: L = a * a (a is used twice) with a = 3. What is dL/da?",
    options: ["dL/da is exactly 3",
              "dL/da is exactly 9",
              "dL/da is exactly 6"],
    answer: 2, explain: "When a variable feeds multiple paths, gradients accumulate: each branch of the multiply contributes a, giving 2a = 6 -- exactly why micrograd uses += in backward." },
  { topic: 'b2', q: "Why can a deep network beat a wide shallow one at equal parameter count?",
    options: ["Depth composes features hierarchically, reusing parts across combinations exponentially",
              "Deeper networks always have strictly lower training loss guaranteed",
              "Shallow networks cannot represent nonlinear functions of their inputs"],
    answer: 0, explain: "Layers build features from earlier features, so deep nets express certain functions with exponentially fewer units; one hidden layer is already a universal approximator, just inefficiently." },
  { topic: 'b2', q: "Which one-paragraph pitch best captures why CNNs suit images?",
    options: ["They process pixels sequentially, remembering earlier pixels through hidden state",
              "They fully connect every pixel pair to maximize model capacity",
              "They share small local filters across positions, exploiting translation invariance"],
    answer: 2, explain: "Convolutions apply the same small kernel everywhere, encoding locality and translation invariance while cutting parameters versus dense layers; sequential hidden state is the RNN story." },
  { topic: 'b2', q: "What is the core purpose of an RNN's hidden state?",
    options: ["It stores gradients between training iterations for momentum updates",
              "It carries a summary of past inputs across timesteps",
              "It caches batch statistics for normalization at inference time"],
    answer: 1, explain: "An RNN feeds a hidden vector from one timestep to the next, giving the network memory of the sequence seen so far." }
];
