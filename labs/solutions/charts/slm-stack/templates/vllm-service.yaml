apiVersion: v1
kind: Service
metadata:
  name: vllm
  namespace: {{ .Values.namespace }}
  labels: { app: vllm }
spec:
  selector: { app: vllm }
  ports:
  - name: http
    port: {{ .Values.vllm.service.port }}
    targetPort: 8000
