# Lab 01 — cluster definition (kind create cluster --config …)
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: llm-stack
nodes:
  - role: control-plane
    extraPortMappings:
      - containerPort: 30080   # NodePort for Open WebUI (lab 08)
        hostPort: 30080
  - role: worker
